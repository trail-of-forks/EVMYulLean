import EvmYul.Yul.Interpreter
import EvmYul.Yul.ReasoningPrinciple
import EvmYul.Yul.YulNotation

\<imports>

namespace \<namespace>

open EvmYul
open EvmYul.Yul
open EvmYul.Yul.Ast
open EvmYul.Yul.Notation
open EvmYul.Yul.ReasoningPrinciple

theorem \<statement_name>_correct
    {fuel : Nat} {codeOverride : Option YulContract} {s₀ s₉ : State} :
    exec fuel (.Block \<statement_name>.body) codeOverride s₀ = .ok s₉ →
    Spec_\<statement_name> fuel codeOverride s₀ s₉ := by
  intro h
  exact function_exec_implies_fuelSpec
    (AFunc := AFunc_\<statement_name> codeOverride)
    \<statement_name>_bodyExact_implies_afunc
    h

end \<namespace>
